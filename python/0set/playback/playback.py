# solution for Playback speed 

# grab userinput and replace whitespace with "..." then print changed string
def main():
	user_input = input("").replace(" ", "...")
	print(user_input) 

# only run when we want it to run 
if __name__ == "__main__":
	main()
